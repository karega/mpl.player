/* @flow */

import {StyleSheet} from 'react-native';

const routeDriverStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        justifyContent: 'flex-start',
        height: 28,
        paddingLeft: 16
    },
    route: {
        color: '#000000',
        fontSize: 16
    },
    driver: {
        color: '#000000',
        fontSize: 16,
        opacity: 0.59
    }
});

export default routeDriverStyles;
