/* browser-reducers.js */

/** External Module Dependencies **/
import Immutable from 'immutable';

const browser = function (browser: Immutable.Map<string, any>, action: Object): Immutable.Map<string, any> {
	switch (action.type) {
		default:
			return browser;
	}
};

export default browser;
